<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SettingsActivity" >

    <TextView 
        android:id="@+id/extendEnd"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:text="@string/text_extendEnd" />

    <CheckBox
        android:id="@+id/extendEndCheck"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/extendEnd" />
    
    <Button
        android:id="@+id/changePassword"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:onClick="newPassword"
        android:layout_above="@+id/settingSave"
        android:text="@string/text_change_password" />
    
    <Button 
        android:id="@+id/settingSave"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:onClick="save" 
        android:layout_above="@+id/settingCancel"
        android:text="@string/text_save" />
    
    <Button 
        android:id="@+id/settingCancel"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:onClick="cancel" 
        android:layout_alignParentBottom="true"
        android:text="@string/text_cancel" />

</RelativeLayout>